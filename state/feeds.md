# Discovered feeds — the agent's cache

The agent writes here automatically. Do not edit by hand.
When the agent finds the real feed behind a normal link, it saves it here so it
does not have to search again tomorrow.

<!-- format: - <original link> => <discovered feed url> -->
